export class Carousal {
  constructor(carousal, images, timeout) {
    this.carousal = carousal;
    this.images = images;
    this.timeout = timeout;
  }

  setup() {
    // This method generates all the divs and the structure for the carousal
  }
}
